# v1.0.4 Build Plan

## Goal
Build the v1.0.4 executable installer for CaféOS POS with all 13 features + 2 bug fixes + BANK_TRANSFER→PayMaya migration.

## Status: Implementation ~90% complete, needs final fixes + build

## Assessment of Current State

### Completed
- BANK_TRANSFER → PAYMAYA migration across all UI and API files
- Schema: `paymayaAccountName`, `paymayaQrImage` fields added; `Supplier` model added; `AuditLog` model added; AI settings fields added
- Prisma: db push done, client generated, DB in sync
- **Critical Bug Fix**: Ingredient stock sync (deduct on order, restore on cancel) — implemented in orders API
- **Loyalty Sync Fix**: Proactive card creation in orders API; `normalizeMemberName` shared utility used in loyalty-award.ts
- **Order Confirmation Dialog**: Present in cart page (lines 477-540)
- **Queue Display**: Present in order status page (lines 505-548)
- **Best Seller API**: `/api/menu/best-sellers` endpoint exists; menu page fetches best seller IDs
- **Graph Download**: Analytics page has html2canvas download buttons
- **Supplier Module**: Schema + API CRUD + inventory management page with suppliers tab
- **Manual Loyalty Points**: Loyalty page has "Add Points" button + modal
- **Audit Logs**: `AuditLog` model, `lib/audit-log.ts` utility, `GET /api/audit-logs`, admin page, nav item; `logAction` calls in orders API, loyalty API, business settings API
- **Consultant Page**: AI chat page + API proxy route + nav item + settings subsection
- **Inventory Threshold**: `editThreshold` state exists in inventory page
- **Ingredients in Inventory**: Inventory page has "Ingredients" tab (redirects to products page for full management)
- **Server-side cancel guard**: Orders PATCH handler validates only PENDING→CANCELLED
- **tsc --noEmit**: Passes (0 errors)

### NOT Yet Done (Remaining Code Changes)

1. **Fix stale "Bank Transfer" text in cart confirmation** (line 496):
   - File: `app/cart/page.tsx:496`
   - Current: `paymentMethod === 'GCASH' ? 'GCash' : 'Bank Transfer'`
   - Should be: `paymentMethod === 'GCASH' ? 'GCash' : 'PayMaya'`

2. **Render Best Seller badge on product cards** (menu page):
   - File: `app/menu/page.tsx` ProductCard function (~line 664-670)
   - `bestSeller` prop is received but never rendered as a badge
   - Add: `{bestSeller && <span className="...🔥 Best Seller...</span>}` near product name

3. **Product inventory threshold input field** (Feature 8):
   - File: `app/admin/inventory/page.tsx` (~line 460)
   - `editThreshold` state is set (line 513) but no input field renders when editing
   - Add input field in the threshold `<td>` column when `isEditing` is true
   - Pass `editThreshold` to saveEdit handler as `lowStockThreshold`

4. **Best Seller badge display** — the `bestSeller` prop is passed to ProductCard but not rendered. Add visual badge.

5. **Audit log missing for inventory + users** (minor):
   - `app/api/inventory/route.ts` — add `logAction` call on adjustments
   - `app/api/users/route.ts` — add `logAction` calls on create/delete
   - (Not blocking for build, can be done post-release)

6. **"Write a review" prompt on order status page** (Feature 4):
   - `app/order/[orderNumber]/page.tsx` — when `order.status === 'COMPLETED'`, show prompt linking to product review section in menu
   - (Minor feature enhancement, not blocking)

### Build Prerequisites
- All remaining source fixes above must be applied before `npm run build`
- `tsc --noEmit` must pass after fixes
- `npx prisma generate` must be run after any schema changes

## Build Steps

### Step 1: Apply remaining source code fixes
```
1. Edit app/cart/page.tsx:496 — Change 'Bank Transfer' → 'PayMaya' in confirmation dialog
2. Edit app/menu/page.tsx — Add bestSeller badge rendering in ProductCard
3. Edit app/admin/inventory/page.tsx — Add threshold input field in edit row
4. (Optional) Add missing audit log calls in inventory/users APIs
```

### Step 2: Run tsc --noEmit
```bash
cd my-app
npx tsc --noEmit
# Must return zero errors
```

### Step 3: Run Prisma generate
```bash
cd my-app
npx prisma generate
```

### Step 4: Run Next.js build
```bash
cd my-app
npm run build
# OR: cross-env ... next build
# Must succeed with no build errors
```

### Step 5: Rebuild NSIS installer
```bash
cd my-app
npm run electron:build:win
# This runs: cross-env CSC_IDENTITY_AUTO_DISCOVERY=false npm run build && electron-builder --win
# OR: cross-env ... next build && electron-builder --win
```

### Step 6: Verify installer
- Check output in `../dist-electron/`
- Verify installer runs and app launches
- Verify PayMaya payment flow works (cart → order page → receipt)

## Validation Checklist
- [ ] `tsc --noEmit` passes with 0 errors
- [ ] `npx prisma generate` succeeds
- [ ] `npm run build` succeeds
- [ ] `electron-builder --win` produces installer
- [ ] "Bank Transfer" text no longer appears anywhere
- [ ] PayMaya QR image displays when PAYMAYA selected in cart
- [ ] PayMaya account name displays in settings
- [ ] PayMaya payment details show on order confirmation
- [ ] Best Seller badge appears on popular product cards in menu
- [ ] Inventory threshold can be edited via input field
- [ ] Order cancel still restricted to PENDING status only
- [ ] Ingredient stock decrements on order, restores on cancel
- [ ] Prospective loyalty card creation works
- [ ] Audit logs page loads with entries
- [ ] Consultant chat page loads and responds
- [ ] Supplier management works in inventory page
